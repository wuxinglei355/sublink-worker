{
  "name": "wayrumble-ecommerce",
  "version": "1.0.0",
  "description": "完整的电商网站和管理系统",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run backend:dev\" \"npm run frontend:dev\" \"npm run admin:dev\"",
    "backend:dev": "cd backend && npm run dev",
    "frontend:dev": "cd frontend && npm start",
    "admin:dev": "cd admin && npm start",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../admin && npm install",
    "build": "cd frontend && npm run build && cd ../admin && npm run build",
    "start": "cd backend && npm start"
  },
  "keywords": ["ecommerce", "react", "nodejs", "mongodb"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
